import React from 'react';
import { SectionContainer } from '../section-container';

export const AboutSection = () => {
  return (
    <SectionContainer title="About Me">
      <div>
        <p>
          I'm a full stack developer who primarily works with Angular in the
          frontend and Node in the backend.
        </p>
      </div>
    </SectionContainer>
  );
};
